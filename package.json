{
  "name": "weixin-websdk-doc",
  "version": "1.0.0",
  "description": "这是weixin-websdk的文档项目",
  "main": "index.js",
  "author": "cuitpanfei",
  "license": "MIT",
   "scripts": {
    "docs:dev": "SET NODE_OPTIONS=--openssl-legacy-provider && vuepress dev docs",
    "docs:build": "SET NODE_OPTIONS=--openssl-legacy-provider && vuepress build docs"
  },
  "devDependencies": {
    "vuepress": "^1.9.9"
  }
}
